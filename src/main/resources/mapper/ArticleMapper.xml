<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atgongda.dao.ArticleMapper">

    <!--<insert id="insertArticle">-->
        <!--insert into article (user_id,article_title,article_desc,article_sort,article_content)-->
        <!--values (#{userId},#{articleTitle},#{articleDesc},#{articleSort},#{articleContent})-->
    <!--</insert>-->

    <insert id="insertArticle">
        insert into article (user_id,article_title,article_desc,article_sort,article_content)
        values (#{userId},#{articleTitle},#{articleDesc},#{articleSort},#{articleContent})
    </insert>
</mapper>

